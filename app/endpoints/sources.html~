<div>
  <form class="form-horizontal" ng-submit="submit()">
    <div class="col-sm-12">
      <form-query-url></form-query-url>
    </div>
    <div class="row">
      <div class="col-sm-4">
        <fetch-button></fetch-button>
      </div>
      
      <div class="col-sm-4">
        <pagination-inputs></pagination-inputs>
      </div>
    </div>
    
  </form>

  <p ng-show="busy">Fetching...</p>
  <p ng-show="results">Showing {{ results.length }} of {{ total }} fetches</p>

  <table class="table table-striped table-bordered">
    <tr>
      <th>url</th>
      <th>adapter</th>
      <th>name</th>
      <th>city</th>
      <th>country</th>
      <th>description</th>
      <th>resolution</th>
      <th>sourceURL</th>
      <th>contacts</th>
    </tr>
    <tr ng-repeat="result in results">
      <td> {{ result.url }}</td>
      <td>{{ result.adapter }}</td>
      <td>{{ result.name }}</td>
      <td>{{ result.city }}</td>
      <td>{{ result.country }}</td>
      <td>{{ result.description }}</td>
      <td>{{ result.resolution }}</td>
      <td>{{ result.sourceURL }}</td>
      <td>
        <ul ng-repeat="contact in result.contacts" class="list-unstyled">
          <li>{{ contact }}</li>
        </ul>
      </td>
    </tr>
  </table>
  
</div>
