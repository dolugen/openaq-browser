# Simple OpenAQ Browser

A web client for [OpenAQ API](https://github.com/openaq/openaq-api/).

## Local setup

```
npm start
```

It will install everything needed and start the server at `http://localhost:8000/`.
